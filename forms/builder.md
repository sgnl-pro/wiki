---
title: FORMS. Конструктор
description: 
published: true
date: 2026-05-15T15:51:23.978Z
tags: 
editor: markdown
dateCreated: 2026-05-15T14:05:05.223Z
---

В этом разделе суперадмин создаёт типы форм и их полей. Формируется библиотека шаблонов, которые потом можно будет использовать в конкретных проектах.

## Типы форм

Каждый тип формы задаёт структуру документа (например, «Акт освидетельствования», «Журнал бетонных работ»). При создании указывается:

1. **Группа форма** (группы создаются здесь же в конструкторе. например `Акты и протоколы`)

1. **Название** – отображаемое имя (например `Акт скрытых работ`)

2. **Префикс** – короткая буквенная метка для идентификации (например, `АОСР`). Она будет автоматически добавляться к номеру экземпляра формы в проекте

3. **Описание** (необязательно)

4. **Порядок** – группы и формы можно перемещать для правильной сортировки

## Типы полей

В каждом типе формы можно создавать список полей следующих типов:
- **Строка**
- **Число**
- **Да/Нет**
- **Дата и время**
- **Диапазон дат**
- **Список вариантов**
- **Файл**
- **Значение из источника**

### Общие свойства любого поля

1. **Название** – отображаемое имя поля в форме

2. **Описание** – вспомогательный текст (отображается пользователю в качестве подсказки при наведении)

3. **Системное имя** – уникальный технический идентификатор для использования в шаблонах генерируемых документов. Допускаются только латинские буквы

4. **Обязательность** – поле требует заполнения перед закрытием формы

5. **Отображение в реестре** – поле отображается в виде колонки в таблице форм проекта

6. **Условие видимости** – поле отображается только если выполнено условие, основанное на значениях других полей этой же формы (например, поле `Причина отказа` показывать только при выборе `Отказано = Да`).

- В условиях видимости можно использовать константы (например `= Да`, `= 100`), сравнение двух полей (например `Дата начала <= Дата окончания`) и группировку условий с `И/ИЛИ`.
- Если поле становится скрыто из-за срабатывания условий, оно не требуется для заполнения даже если является обязательным

7. **Порядок** – поля можно перемещать для правильной очередности отображения

### Дополнительные свойства полей в зависимости от типа

1. 
1. Единица измерения - строка, число
1. Значение по умолчанию - строка, число, дата и время (фиксированное или текущее ± дни), да/нет, список вариантов
1. Время - дата и время
1. Множественный выбор - список вариантов, файл, значение из источника
1. Допустимые значения
- Маска, Ругулярное выражение и Текст ошибки - строка
- От, До, Шаг, Включение границ - число
- набор вариантов
1. список форм и фильтрация по значениям полей - значение из источника

8. Значение из источника	Множественный выбор, связь с формой-источником, фильтрация по равенству полей

Использовать как отображаемое значение – если поле выбрано, его значение будет показываться в списках (реестрах) рядом с номером формы.